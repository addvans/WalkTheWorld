namespace WalkTheWorld
{
    public enum LeavingType
    {
        Everyone,
        Selected,
        AlwaysAsk
    }
    public enum RandomEventsFilterType
    {
        Filtered,
        Unfiltered
    }
    public enum CameraFocusMode
    {
        OnEnteredPawns,
        Centered,
        Ignore
    }
}
